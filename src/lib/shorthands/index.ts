import margin from "./margin";
import padding from "./padding";
import border from "./border";

export const roots = [padding[0], margin[0], border[0]];
export const suffixes = [padding[1], margin[1], border[1]];